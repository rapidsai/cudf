name: gdf
channels:
- numba
- conda-forge
- defaults
dependencies:
- pip
- setuptools
- cmake
# Update based on your host's CUDA driver version
- cudatoolkit=9.2
- numba
- pandas
- flatbuffers
- arrow-cpp=0.7.1
